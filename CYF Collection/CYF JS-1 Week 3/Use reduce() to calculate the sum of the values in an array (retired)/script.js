// my solution
function sum(array) {
  // Use array.reduce() to calculate and return the
  // sum of the values in array.
  array.reduce((prev,curr) => (x) = prev + curr )
  return x
}



//Other solutions





//Test
