function generateHashtag(str) {
  let arr = str
    .split(" ")
    
  return arr
//     ? false
//     : `#${arr
//         .map((x) => x[0].toUpperCase() + x.substring(1, x.length))
//         .join("")}`;
// }
}
console.log(generateHashtag(""));